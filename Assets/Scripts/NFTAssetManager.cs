using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NFTAssetManager : MonoBehaviour {

    [SerializeField] public GameObject farmerOldMan;
    [SerializeField] public GameObject farmerMan;
    [SerializeField] public GameObject farmerLady;
    [SerializeField] public GameObject farmerBuffGuy;
    [SerializeField] public GameObject farmerTeenChic;

    public bool oldMan, man, lady, buffGuy, teenChic;

    // Start is called before the first frame update
    void Start() {
        
    }

    // Update is called once per frame
    void Update() {
        
    }
}
